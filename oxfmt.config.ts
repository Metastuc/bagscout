import { defineConfig } from "oxfmt";

export default defineConfig({
    endOfLine: "crlf",
    printWidth: 180,
    semi: true,
    singleQuote: false,
    tabWidth: 4,
    trailingComma: "none",
    useTabs: false
});
